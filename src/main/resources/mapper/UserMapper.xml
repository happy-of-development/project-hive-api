<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hod.project.hive.mapper.UserMapper">
    <select id="findUser" parameterType="string" resultType="com.hod.project.hive.entity.User">
        SELECT u.id id, u.name name, u.photo photo, u.mobile mobile, u.email email, t.name teamName FROM tb_user u,
            (SELECT tb_team.name FROM tb_team,  tb_team_user WHERE tb_team_user.user_id =#{id} AND tb_team.id = tb_team_user.team_id) t WHERE u.id=#{id};

    </select>

</mapper>